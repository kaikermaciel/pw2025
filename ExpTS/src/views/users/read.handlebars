<div class="d-flex justify-content-between align-items-center">
    <div class="card" style="width: 20rem;">
    <div class="card-body">
        <h5 class="card-title">{{user.name}}</h5>
        <h6 class="card-subtitle mb-2 text-body-secondary">{{user.email}}</h6>
        <a href="/users/update/{{user.id}}" class="card-link">Editar Informações</a>
        <a href="/users/remove/{{user.id}}" class="card-link">Apagar Usuário</a>
    </div>
    </div>
    <h4><a href="/users"><i class="bi bi-arrow-left-square"></i> </a></h4>
</div>